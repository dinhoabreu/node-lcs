{
  "name": "node-lcs",
  "version": "1.0.2",
  "description": "Implementations for the Longest Common Substring algorithm [O(nm) time]",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=text-summary --reporter=lcovonly mocha && npm run coveralls",
    "coveralls": "if-env TRAVIS=true && coveralls <./coverage/lcov.info || true"
  },
  "author": "Edison E. Abreu",
  "keywords": [
    "lcs",
    "longest",
    "common",
    "substring"
  ],
  "license": "MIT",
  "devDependencies": {
    "coveralls": "^3.0.0",
    "if-env": "^1.0.4",
    "mocha": "^4.0.1",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^11.3.0",
    "standard": "^10.0.3"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/dinhoabreu/node-lcs.git"
  }
}
